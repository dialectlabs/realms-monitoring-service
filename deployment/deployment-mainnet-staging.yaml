apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "16"
  labels:
    app: squads-monitoring-service-mainnet-staging
  name: squads-monitoring-service-mainnet-staging
  namespace: default
  resourceVersion: "65918483"
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: squads-monitoring-service-mainnet-staging
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: squads-monitoring-service-mainnet-staging
    spec:
      containers:
      - env:
        - name: NETWORK_NAME
          value: "mainnet"
        - name: NX_ENVIRONMENT
          value: "mainnet_staging"
        - name: RPC_URL
          valueFrom:
            secretKeyRef:
              key: MAINNET_RPC_URL
              name: env-vars
        - name: PRIVATE_KEY
          valueFrom:
            secretKeyRef:
              key: SQUADS_MESSAGING_PRIVATE_KEY_MAINNET_STAGING
              name: env-vars
        image: dialectlab/squads-monitoring-service:0.1.0
        imagePullPolicy: Always
        name: squads-monitoring-service-mainnet-staging
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      imagePullSecrets:
      - name: regcred
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
